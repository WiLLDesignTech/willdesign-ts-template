{
  "name": "willdesign-project-name",
  "version": "0.1.0",
  "private": true,
  "description": "TODO: Add project description",
  "engines": {
    "node": ">=22.12.0",
    "pnpm": ">=9.0.0"
  },
  "packageManager": "pnpm@10.14.0",
  "scripts": {
    "prepare": "husky",
    "commitlint": "commitlint --edit",
    "lint": "echo 'Add eslint config and update this script'",
    "lint:fix": "echo 'Add eslint config and update this script'",
    "typecheck": "echo 'Add tsconfig.json and update this script'",
    "test": "echo 'No tests configured yet' && exit 0",
    "build": "echo 'Add build script for your project' && exit 0",
    "format": "prettier --write '**/*.{ts,tsx,json,css,md}'",
    "format:check": "prettier --check '**/*.{ts,tsx,json,css,md}'"
  },
  "devDependencies": {
    "@commitlint/cli": "^20.5.0",
    "@commitlint/config-conventional": "^20.5.0",
    "@commitlint/types": "^20.5.0",
    "eslint": "^10.2.0",
    "husky": "^9.0.0",
    "lint-staged": "^16.4.0",
    "prettier": "^3.8.0",
    "typescript": "^6.0.2"
  }
}
